<UserControl
    x:Class="IIens.View.NewsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IIens.View"
    xmlns:src="clr-namespace:IIens"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <ListView HorizontalAlignment="Left" Height="auto" VerticalAlignment="Top" Width="auto"  ItemsSource="{Binding}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Background="DarkOrange" Width="460" Height="300" Margin="0,10,0,0">
                        <StackPanel  Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Path=titre, Mode=OneWay}" />
                                <TextBlock Text="{Binding Path=par, Mode=OneWay}" />
                                <TextBlock Text="{Binding Path=calDate, Mode=OneWay}" />
                            </StackPanel>
                            <TextBlock Text="{Binding Path=contenu, Mode=OneWay}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
